# Токен от @BotFather. Обязательный параметр.
BOT_TOKEN=

# Файл базы SQLite.
DATABASE_PATH=bot.db

# Как часто пересматривать список товаров.
# Меньше 10 минут DNS, как правило, воспринимает как бота.
CHECK_INTERVAL=20m

# Пауза между карточками DNS внутри одного цикла.
FETCH_GAP=45s

# Сколько карточек максимум трогать за один цикл.
FETCH_PER_CYCLE=8

# Не ходить на DNS сразу после запуска процесса.
STARTUP_DELAY=1m

# Сколько молчать, если QRATOR показал челлендж или оборвал соединение.
CIRCUIT_COOLDOWN=45m

# Профиль Chrome, чтобы cookie челленджа переживали перезапуск.
CHROME_PROFILE=data/chrome-profile

# Пусто — искать Google Chrome в стандартном месте.
CHROME_PATH=

# 1, чтобы спрятать окно. На DNS headless часто не проходит челлендж.
CHROME_HEADLESS=0

# Город, для которого фиксируются цены: на DNS цена зависит от региона.
DEFAULT_CITY=moscow

# Список Telegram user id через запятую. Пусто — доступ открыт всем.
ALLOWED_USERS=

# Локальная страница со всеми заявками. off — не поднимать HTTP.
UI_ADDR=127.0.0.1:8080

# Окно со списком ссылок и графиками цен + иконка в трее.
# off — только Telegram и веб-страница.
GUI=1
